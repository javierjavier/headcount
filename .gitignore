# Photo data — never commit (huge, and contains a child's face/GPS data)
album/
matches/
*-matches/
reference/
known-positives/

# Derived biometric data + generated artifacts
reference_embeddings.npy
scores.csv
# whole-class face database (clustering pipeline) — every child's biometric data
faces*.emb
faces*.npy
faces*.csv
faces*.done
clusters*.csv
labels*.csv
image_people*.csv
scene*.csv
# backups of the above (review/cluster write *.bak) — same biometric content
*.bak
embed_full.log
clusters*/
by_child*/
query*/
validation_*.jpg
*.log
# local `serve` thumbnail cache — derived face thumbnails
.serve_cache/

# Python
__pycache__/
*.pyc
.venv/
venv/

# macOS
.DS_Store
